package repositories;


import entities.ResponsablePedagogique;

public class ResponsablePedagogiqueRepository {
    ResponsablePedagogique responsablePedagogique=new ResponsablePedagogique();

    /*public void insertClasse(Classe classe){
        openConnexion();
        try {
            initPreparedStatement(SQL_INSERT);
            statement.setString(1, etudiant.getMatricule());
            statement.setString(2, etudiant.getNomcomplet());
            statement.setString(3, etudiant.getTuteur());
            statement.setDate(3, new java.sql.Date(new Date().getTime()));
            statement.setString(4, etudiant .getTuteur());
            int nbreLigne=executeUpdate();
        closeConnexion();
        } catch (SQLException e) {
        e.printStackTrace();
        }
   }
*/


    
}
